1 64 位也受检测

2 未加密
	运行		X
	只加载不运行	X

3 加密
	运行		X
	只加载不运行	O

修改窗口文字可检测。所以应该不是检测的窗口相关的文字
结论：因为代码段已虚拟化，初步认为不是用的代码段特征字。应该是常量特征字

在UI编辑界面中，MainForm会被检测到

Panel5是关键

今天发现
窗口位置不重要，文字不重要。估计是用窗口类进行模糊匹配
但是操蛋的是lazarus不支持修改窗口类，找了一天都说要修改基础类库，太蛋疼了。

作为数据读到内存时没报
作为DLL加载时没报（但是不能执行，还在找原因，好像是重定位没有执行）
用调试器加载，停在入口也会报。

今天测试：
修改模块链，将主模块删除，修改peb将imagebase改成ntdll，erase section table, 全部没有躲过检测

'.rdata' 节好像是主要特征字所在位置， 清0后过检测

.rdata
全部清0			O
前 1/2 清0		O
前 1/4 清0		O
前 1/8 清0		O
前 1/16 清0		X

初步说明特征字在前1/8的位置, 并且在这1/8的后半部分的第2个1/16的区域内

覆盖第2个1/16的第一个1/32没有通过测试， 第2个1/32也没有通过测试
